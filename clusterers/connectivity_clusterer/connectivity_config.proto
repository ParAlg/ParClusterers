syntax = "proto2";

package research_graph.in_memory;

message ConnectivityClustererConfig {
  optional double threshold = 1 [default = inf];
  optional bool upper_bound = 2 [default = true];
  optional double ratio = 3 [default = -1];
}